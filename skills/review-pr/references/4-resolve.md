# Step 4b: Resolve Issues

Fix selected issues.

## Execute

Run `implement` agents:
- Different files: parallel
- Same file with dependencies: batch into one agent
- Same file without dependencies: parallel

## Next

- `-ss` flag: stop
- Otherwise: read `~/.claude/skills/review-pr/references/5-save.md`
