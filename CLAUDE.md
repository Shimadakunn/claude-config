# Git Commits

- Never include `Co-Authored-By` trailers in commit messages

# Frontend Development

- When implementing frontend features, use the `frontend-design` skill

# Codebase Exploration

- When exploring the codebase, use Serena MCP tools instead of grep
